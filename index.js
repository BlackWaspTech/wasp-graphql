var query = require('./src/query');
var mutate = require('./src/mutate');
var subscribe = require('./src/subscribe');

module.exports = {
  query: query,
  mutate: mutate,
  subscribe: subscribe
};
